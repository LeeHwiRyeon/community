# 프로덕션 환경 설정 예시
# 이 파일을 .env.prod로 복사하여 실제 값으로 변경하세요

# 서버 설정
NODE_ENV=production
PORT=50000
HOST=0.0.0.0

# 데이터베이스 설정 (프로덕션용)
DB_HOST=prod-db-server
DB_PORT=3306
DB_NAME=community_prod
DB_USER=app
DB_PASS=prod_database_password

# Redis 설정 (프로덕션용)
REDIS_HOST=prod-redis-server
REDIS_PORT=6379
REDIS_PASSWORD=prod_redis_password

# JWT 설정
JWT_SECRET=prod_jwt_secret_key_here
JWT_EXPIRES_IN=24h

# OAuth 설정 (프로덕션용)
OAUTH_GOOGLE_CLIENT_ID=prod_google_client_id
OAUTH_GOOGLE_CLIENT_SECRET=prod_google_client_secret
OAUTH_GITHUB_CLIENT_ID=prod_github_client_id
OAUTH_GITHUB_CLIENT_SECRET=prod_github_client_secret

# 로깅 설정
LOG_LEVEL=warn
LOG_JSON=1

# CORS 설정
CORS_ORIGIN=https://community-hub.com,https://www.community-hub.com

# 모니터링 설정
HTTP_BUCKETS=100,500,1000,2000,5000

# 시크릿 관리 (GitHub Secrets에서 설정)
# SLACK_WEBHOOK_URL
# DISCORD_WEBHOOK_URL

# 프로덕션 전용 설정
RATE_LIMIT_WRITE_PER_MIN=60
RATE_LIMIT_SEARCH_PER_MIN=120
DISABLE_HELMET=0
DISABLE_CSP=0