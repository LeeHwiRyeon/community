# 스테이징 환경 설정 예시
# 이 파일을 .env.staging로 복사하여 실제 값으로 변경하세요

# 서버 설정
NODE_ENV=staging
PORT=50000
HOST=0.0.0.0

# 데이터베이스 설정 (스테이징용)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=community_staging
DB_USER=app
DB_PASS=staging_password

# Redis 설정 (스테이징용)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT 설정
JWT_SECRET=staging_jwt_secret_key_here
JWT_EXPIRES_IN=24h

# OAuth 설정 (스테이징용)
OAUTH_GOOGLE_CLIENT_ID=staging_google_client_id
OAUTH_GOOGLE_CLIENT_SECRET=staging_google_client_secret
OAUTH_GITHUB_CLIENT_ID=staging_github_client_id
OAUTH_GITHUB_CLIENT_SECRET=staging_github_client_secret

# 로깅 설정
LOG_LEVEL=info
LOG_JSON=1

# CORS 설정
CORS_ORIGIN=http://localhost:3000,https://staging.community-hub.com

# 모니터링 설정
HTTP_BUCKETS=100,500,1000,2000,5000

# 시크릿 관리 (GitHub Secrets에서 설정)
# SLACK_WEBHOOK_URL
# DISCORD_WEBHOOK_URL