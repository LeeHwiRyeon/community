# Deploy to Production - Security Hardened
name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: '20'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # 보안 검사 및 테스트
  security-and-test:
    runs-on: ubuntu-latest
    outputs:
      should-deploy: ${{ steps.decision.outputs.should-deploy }}
    
    steps:
    - name: 코드 체크아웃
      uses: actions/checkout@v4

    - name: Node.js 설정
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: 보안 감사
      run: |
        npm audit --audit-level=high
        cd server-backend && npm audit --audit-level=high
        cd ../frontend && npm audit --audit-level=high

    - name: 의존성 설치
      run: |
        npm ci --no-audit --no-fund
        cd frontend && npm ci --no-audit --no-fund
        cd ../server-backend && npm ci --no-audit --no-fund

    - name: 보안 테스트
      run: |
        cd server-backend
        npm run test:security

    - name: 빌드 테스트
      run: |
        cd frontend && npm run build
        cd ../server-backend && npm run build

    - name: 배포 결정
      id: decision
      run: |
        if [[ "${{ github.event_name }}" == "workflow_dispatch" ]] || [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
          echo "should-deploy=true" >> $GITHUB_OUTPUT
        else
          echo "should-deploy=false" >> $GITHUB_OUTPUT
        fi

  # Docker 이미지 빌드 및 푸시
  build-and-push:
    needs: security-and-test
    if: needs.security-and-test.outputs.should-deploy == 'true'
    runs-on: ubuntu-latest
    
    steps:
    - name: 코드 체크아웃
      uses: actions/checkout@v4

    - name: Docker 로그인
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: 메타데이터 추출
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: 백엔드 이미지 빌드 및 푸시
      uses: docker/build-push-action@v5
      with:
        context: ./server-backend
        file: ./server-backend/Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}-backend
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        platforms: linux/amd64,linux/arm64

    - name: 프론트엔드 이미지 빌드 및 푸시
      uses: docker/build-push-action@v5
      with:
        context: ./frontend
        file: ./frontend/Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}-frontend
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        platforms: linux/amd64,linux/arm64

    - name: Docker 보안 스캔
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ steps.meta.outputs.tags }}-backend
        format: 'sarif'
        output: 'trivy-results.sarif'

    - name: Trivy 결과 업로드
      uses: github/codeql-action/upload-sarif@v2
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'

  # 배포 (실제 배포 로직은 환경에 따라 수정 필요)
  deploy:
    needs: [security-and-test, build-and-push]
    if: needs.security-and-test.outputs.should-deploy == 'true'
    runs-on: ubuntu-latest
    environment: production
    
    steps:
    - name: 코드 체크아웃
      uses: actions/checkout@v4

    - name: 배포 알림
      run: |
        echo "🚀 배포 준비 완료!"
        echo "백엔드 이미지: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-backend"
        echo "프론트엔드 이미지: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-frontend"
        echo "배포 환경: production"
        
    # 실제 배포 스크립트는 여기에 추가
    # 예: Kubernetes, Docker Swarm, 또는 클라우드 서비스 배포